import React, { PropTypes } from 'react';
import styles from '../../styleSheets/header/mainPageHeader.css';

const MainPageHeader = (props) => {

    return (
        <div className={styles.header}>
            <h1> Main Page</h1>
        </div>
    );
}

export default MainPageHeader;